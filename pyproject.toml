[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "profsea"
version = "3.0"

[tool.setuptools.packages.find]
include = ["profsea*"]
exclude = ["docs*", "tests*"]
